package com.courier.modules.support.domain;

/** Influences SLA (Phase 2) and default sort order in the ticket list — highest first. */
public enum TicketPriority {
    LOW,
    MEDIUM,
    HIGH,
    CRITICAL
}
